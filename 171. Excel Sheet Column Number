public class Solution {
    public int TitleToNumber(string columnTitle) {
        int res = 0;

        foreach(var v in columnTitle)
        {
            res = res*26 + (v - 'A'+1);
        } return res;
    }
}
