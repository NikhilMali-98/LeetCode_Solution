public class Solution {
    public int LengthOfLastWord(string s) {
        s = s.Trim();
        string[] str = s.Split(' ');
        string last =   str[str.Length-1];
        return last.Length;
    }
}
