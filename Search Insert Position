public class Solution {
    public int SearchInsert(int[] nums, int target) {
        int val = 0;
        for(int i=0; i<nums.Length; i++)
        {
            if(nums[i]>= target) return val = i;
        } return nums.Length;
    }
}
